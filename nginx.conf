server {
    listen 9090;
    server_name _;

    location / {
        root /opt/MealPlannerFrontend/;
        index index.html;
        try_files $uri $uri/ @backend;
    }

    location @backend {
        proxy_pass http://192.168.178.75:8080;
    }
}